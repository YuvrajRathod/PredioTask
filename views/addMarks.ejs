<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Marks</title>
    <link rel="stylesheet" href="/styles/update.css">

</head>
<body>
    <%- include('partials/nav') %>
    <main>
        <form action="/addStudentMarks" method="post" id="studentMarksForm">
            
            <%subjectArray.forEach((subject,index)=>{%>
            <label for="<%=subject%>"><%=subject%>:</label>

            <% if (flag) { %>
                <input type="number" id="<%= subject %>ob" name="<%= subject %>ob" value="<%= marksList[index][0] %>" required><br><br>
            <% } else { %>
                <input type="number" id="<%= subject %>ob" name="<%= subject %>ob" value="00" required><br><br>
            <% } %>
            
            <% if (flag) { %>
                <input type="number" id="<%= subject %>fm" name="<%= subject %>fm" value="<%= marksList[index][1] %>" required><br><br>
            <% } else { %>
                <input type="number" id="<%=subject%>fm" name="<%=subject%>fm" value="100" required><br><br>
            <% } %>
         
            <%})%>
            <input style="display: none;" type="text" name="studentID" value="<%=studentID%>">
            <button type="submit">Add Marks</button>
        </form>
    </main>
    
    <%- include('partials/footer') %>
</body>
</html>